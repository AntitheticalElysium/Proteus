"""Proteus — A Mechanistic Taxonomy of Hallucination.

This package is intentionally empty during Phase 1. Phase 1 drives the
upstream `vendor/sae_entities` pipeline via shell shims under `scripts/`
and adds no original Python code. Modules are added here in Phase 2 when
we extend Ferrando's setup to the other four hallucination types.
"""

__version__ = "0.1.0"
